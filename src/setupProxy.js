module.exports = function (app) {
  // Add any custom middleware setup here if needed in the future
  // This file's presence will trigger the new middleware setup pattern
  // and resolve the deprecation warnings
};
